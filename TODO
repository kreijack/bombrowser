

- create "revise/copy" code
- create "the edit dialog"
- when a code is selected, show all the dates combining all the children
- in listcodegui handle the case where the code has different descriptions for
  different revisions
- in codegui show the revision_id instead of code_id

- remake the tests



-----



- create 'edit code' window
- create 'new code' window
- create 'update parents' dialog
- add support for develop bom
- refactory database (v0.3 ?)
	- create up to 10 (?) generic fields to items row and remove the
	  current already specialized ones. The field name should be
	  configured in the database 'props' tables
